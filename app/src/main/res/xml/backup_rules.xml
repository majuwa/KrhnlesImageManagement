<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude the credentials DataStore to prevent server URL leaking via Google backup -->
    <exclude domain="file" path="datastore/credentials.preferences_pb"/>
</full-backup-content>